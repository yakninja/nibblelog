# User credentials (username:password format, comma-separated)
NIBBLE_USERS=yak:changeme

# JWT secret key (change in production)
JWT_SECRET=dev-secret-change-me-in-production

# Database connection
DATABASE_URL=postgresql://nibble:nibble@localhost:5432/nibblelog

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:19006,http://localhost:8081,http://localhost:3000,https://nibble.yakninja.pro
